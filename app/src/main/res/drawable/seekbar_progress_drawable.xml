<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!--定义seekbar滑动条的底色-->
    <item android:id="@android:id/background">
        <shape>
            <corners android:radius="30dp" />
            <gradient
                android:angle="270"
                android:centerColor="@color/text_gray_bg_color"
                android:endColor="@color/text_gray_bg_color"
                android:startColor="@color/text_gray_bg_color" />
        </shape>
    </item>
    <!--定义seekbar滑动条进度颜色-->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <corners android:radius="30dp"/>
                <solid android:color="@color/text_blue_bg_color"/>
            </shape>
        </clip>
    </item>
</layer-list>
